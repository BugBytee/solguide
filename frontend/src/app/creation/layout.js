export const metadata =
{
    title: "Creation",
    description: "Get registered and start collaborating",
}

export default function CreationLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}